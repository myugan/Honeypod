{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "honeypod.serviceAccountName" . }}
  namespace: {{ include "honeypod.namespace" . }}
  labels:
    {{- include "honeypod.labels" . | nindent 4 }}
{{- end }}
